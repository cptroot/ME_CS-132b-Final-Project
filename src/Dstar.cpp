#include <stdio.h>
#include <queue>

Dstar::Dstar() {
    int world_x;
    int world_y;
    
    int start;
    int goal;
    int size;
    std::priority_queue<cell> pqueue;
}

cell Dstar::get(n) {
    
}

void Dstar::put(n, cell) {
    
}

void Dstar::get_open() {
    if (!pqueue.empty()) {
        cell item = pqueue.top();
        pqueue.pop();
        cu
        
    }
    else {
        
    }
}